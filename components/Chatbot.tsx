import React from 'react';

// The Chatbot component has been removed as per user request.
// This file is kept to avoid breaking the file system structure, but its content is cleared.
const Chatbot: React.FC = () => {
  return null;
};

export default Chatbot;